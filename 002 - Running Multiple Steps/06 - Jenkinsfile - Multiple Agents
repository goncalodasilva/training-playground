pipeline {
    agent none
    
    stages {
    	stage('maven') {
		agent { docker { image 'maven:latest' } }
		steps {
			mvn --version
		}
	}
	stage('node') {
		agent { docker { image 'node:latest' } }
		steps {
			npm --version
		}
	}
    }
}
