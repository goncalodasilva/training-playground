pipeline {
    agent none
    
    stages {
    	stage('maven') {
		agent { docker { image 'alpine:latest' } }
		steps {
			mvn --version
		}
	}
	stage('node') {
		agent { docker { image 'node:latest' } }
		steps {
			npm --version
		}
	}
    }
}
